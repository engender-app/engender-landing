{
  "name": "gender-diary-landing",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "prepare": "svelte-kit sync",
    "dev": "vite dev",
    "build": "vite build && node scripts/crawl-policy.mjs",
    "build:production": "SITE_ENV=production npm run build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "links": "npm run build && node scripts/check-links.mjs",
    "test": "npm run build && node tests/site.test.mjs && node tests/policy.test.mjs"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.0",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/node": "^26.2.0",
    "playwright-core": "^1.62.1",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
